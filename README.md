# webServer_TFG

git clone https://github.com/Calafi/webServer_TFG.git

go mod init webServer_TFG

go build -o out && ./out (compilar y ejecutar)
go run . (ejecuta un ejecutable temporal compilado en el momento)
